# Project Configuration

Every configurable value lives in `.env` (copy from `.env.example`) and/or `src/config/*.ts`. Nothing in this app reads configuration from anywhere else. Server-only variables (no `VITE_` prefix) are never sent to the browser; `VITE_*` variables are inlined into the client bundle at build time and must never hold a secret.

## Environment variables

| Variable                                                                                                                                                                                             | Server/Client | Purpose                                                                                                 | Current state                                         |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| `VITE_APP_NAME`                                                                                                                                                                                      | Client        | Site name shown in header/meta                                                                          | `RobinPulse AI`                                       |
| `COINGECKO_API_KEY`                                                                                                                                                                                  | Server        | Optional CoinGecko API key (demo or pro)                                                                | Empty — free tier works unauthenticated, rate-limited |
| `COINGECKO_API_BASE_URL`                                                                                                                                                                             | Server        | CoinGecko REST base URL                                                                                 | `https://api.coingecko.com/api/v3`                    |
| `DEXSCREENER_API_BASE_URL`                                                                                                                                                                           | Server        | DEX Screener REST base URL                                                                              | `https://api.dexscreener.com`                         |
| `GECKOTERMINAL_API_BASE_URL`                                                                                                                                                                         | Server        | GeckoTerminal REST base URL                                                                             | `https://api.geckoterminal.com/api/v2`                |
| `GECKOTERMINAL_ROBINHOOD_NETWORK_ID`                                                                                                                                                                 | Server        | GeckoTerminal network slug for Robinhood Chain                                                          | `robinhood` (confirmed live)                          |
| `ROBINHOOD_CHAIN_ID`                                                                                                                                                                                 | Server        | Robinhood Chain numeric chain ID, used by `/api/market/status`                                          | `4663`                                                |
| `ROBINHOOD_CHAIN_NAME` / `ROBINHOOD_RPC_URL` / `ROBINHOOD_EXPLORER_URL` / `ROBINHOOD_NATIVE_CURRENCY_NAME` / `ROBINHOOD_NATIVE_CURRENCY_SYMBOL`                                                      | Server        | Reference copies of the chain facts                                                                     | Set (see docs/ADDING_ROBINHOOD_NETWORK.md)            |
| `VITE_ROBINHOOD_CHAIN_ID` / `VITE_ROBINHOOD_CHAIN_NAME` / `VITE_ROBINHOOD_RPC_URL` / `VITE_ROBINHOOD_EXPLORER_URL` / `VITE_ROBINHOOD_NATIVE_CURRENCY_NAME` / `VITE_ROBINHOOD_NATIVE_CURRENCY_SYMBOL` | Client        | Client-safe copies of the same public chain facts, consumed by `src/config/project.ts`'s `wallet` block | Set                                                   |
| `VITE_ROBINHOOD_DEXSCREENER_CHAIN_ID`                                                                                                                                                                | Client        | DEX Screener chain slug that qualifies as "Robinhood Mainnet"                                           | `robinhood` (confirmed live)                          |
| `VITE_WALLETCONNECT_PROJECT_ID`                                                                                                                                                                      | Client        | WalletConnect Cloud project ID                                                                          | Empty — wallet stays disabled until set               |
| `VITE_PROJECT_WEBSITE_URL` / `VITE_PROJECT_DOCS_URL` / `VITE_PROJECT_WHITEPAPER_URL`                                                                                                                 | Client        | Footer/links                                                                                            | Empty                                                 |
| `VITE_X_URL` / `VITE_DISCORD_URL` / `VITE_TELEGRAM_URL` / `VITE_GITHUB_URL` / `VITE_MEDIUM_URL`                                                                                                      | Client        | Social links — hidden unless set                                                                        | Empty                                                 |
| `VITE_CONTACT_EMAIL` / `VITE_SUPPORT_URL`                                                                                                                                                            | Client        | Contact links                                                                                           | Empty                                                 |

## Config files (`src/config/`)

| File                | Governs                                                                                       |
| ------------------- | --------------------------------------------------------------------------------------------- |
| `project.ts`        | App name, social links, wallet readiness, data-source flags — reads only `VITE_*` vars        |
| `genesisAgents.ts`  | Agent Hub's six Genesis Agent economics/lore (see docs/AGENT_ECONOMY_CONFIG.md)               |
| `agentHub.ts`       | Agent Hub mode, complete-set-bonus, marketplace/fusion flags (see docs/AGENT_HUB_OVERVIEW.md) |
| `contracts.ts`      | Agent Hub contract addresses — empty until deployed (see docs/AGENT_CONTRACT_INTEGRATION.md)  |
| `tge.ts`            | TGE status and $PULSE claim/distribution mode (see docs/PULSE_CLAIM_SETUP.md)                 |
| `verifiedTokens.ts` | Owner-verified on-chain token allowlist (see docs/ADDING_VERIFIED_TOKENS.md)                  |
| `trending.ts`       | Trend-score weights, safeguards, cache TTLs (see docs/SCORING_ENGINE.md)                      |
| `token.ts`          | The `/token` announcement page (see docs/TOKEN_PAGE_SETUP.md)                                 |

Server secrets (API keys) are read directly from `process.env` inside `src/lib/market/*.server.ts` — they are never re-exported through `project.ts` or any client-reachable module.
